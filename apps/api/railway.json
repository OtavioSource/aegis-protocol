{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @command-rail/shared build && pnpm --filter @command-rail/policy-engine build && pnpm --filter @command-rail/solana build && pnpm --filter @command-rail/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @command-rail/api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE"
  }
}
