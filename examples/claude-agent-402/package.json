{
  "name": "claude-agent-402",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Demo Claude tool_use + HTTP 402 + Aegis payment",
  "scripts": {
    "start": "dotenv -e .env.local -- tsx src/agent.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@aegis/sdk": "workspace:*",
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "dotenv-cli": "^8.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
