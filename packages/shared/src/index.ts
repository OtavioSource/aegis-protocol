/**
 * @aegis/shared — types, enums, Zod schemas e interfaces compartilhadas.
 *
 * Conteúdo planejado (iteração 2):
 * - types.ts:    SpendRequest, Policy, Decision, RuntimeContext, SettlementAdapter
 * - enums.ts:    SpendRequestStatus, Decision, EventType, etc.
 * - schemas.ts:  Zod schemas para validação na borda da API
 */

export const SHARED_PACKAGE_VERSION = '0.0.1';
